<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NT118P21_22520047_Lab04"
        tools:targetApi="31">
        <activity
            android:name=".SQLite"
            android:exported="false" />
        <activity
            android:name=".ExternalStorage"
            android:exported="false" />
        <activity
            android:name=".InternalStorage"
            android:exported="false" />
        <activity
            android:name=".Setting"
            android:exported="false" />
        <activity
            android:name=".SharedPreferences"
            android:exported="false" /> <!-- Màn hình đầu tiên chạy app -->
        <activity
            android:name=".LearnPreferenceLayout"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>